---
label: Post - Single
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
  description: The name of the post. Used to generate the URL as well.
- name: date
  type: datetime
  label: Date
  description: ''
  config:
    required: true
    date_format: 
    time_format: 
    display_utc: false
  default: now
- name: eleventyExcludeFromCollections
  type: boolean
  label: Hide
  description: Toggling this on will prevent the post from appearing on the site.
    It defaults to "on"—turn it off to publish a post.
  default: true
- name: blocks
  type: blocks
  label: Content
  template_types:
  - block-text
  - block-image
  - block-split
  - block-callout
  - block-decoration
  config:
    min: 1
    max: 
  description: Assemble some blocks to make your content.
