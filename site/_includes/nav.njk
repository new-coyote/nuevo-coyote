<nav class="p-6 mb-16">
    <ul class="flex justify-center -skew-y-3 transform items-center">
        <li class="mr-1 mt-1">
            <a href="/" class="p-1">🐺</a>
        </li>
    {% for item in collections.all | eleventyNavigation %}
        <li class="mx-2 pb-1 px-1 font-heading font-bold {% if item.url == page.url %} border-orange border-b-2 {% endif %}">
            <a href="{{ item.url }}">{{ item.title }}</a>
        </li>
    {% endfor %}
    </ul>
</nav>
